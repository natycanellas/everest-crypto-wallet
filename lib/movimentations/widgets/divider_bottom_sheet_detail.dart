import 'package:flutter/material.dart';

class DividerBottomSheetDetail extends StatelessWidget {
  const DividerBottomSheetDetail({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Divider(
      thickness: 1.3,
      height: 22,
    );
  }
}
